import React from 'react'

export default function SouthernPages() {

    const styles = {        
        fontSize: "35px",
        fontWeight: 300,
        letterSpacing: ".1em",
        filter: "drop-shadow(0 0 2px rgba(0, 0, 0, .9))",
        lineHeight: ".9em",
        paddingBottom: ".3em",
        height: "min-content"
    };

  return (
    <p style={styles}>southern pages</p>
  )
}
